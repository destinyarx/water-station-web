# 001 — Persist document files privately

Implement `REQUIREMENTS.md` with the review migration `002-document-storage-and-visibility.sql`, upload UI validation, service cleanup on partial failure, signed URL access, and service/schema/mapper/guard tests. Database SQL is reviewed and applied manually in the separate Supabase repository.
